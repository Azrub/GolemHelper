Lost access to this account due to a 2FA issue after migrating to a new authenticator app. GitHub support was unable to help despite extensive proof of ownership provided, including: original email address, active OAuth token extracted from Windows Credential Manager via PowerShell, and successful API authentication returning full private account data. I was even still logged in via GitHub Desktop, which is how I was able to push this very commit — wipe the entire repository history and source code by creating an orphan branch, force-pushing it as main, and replacing all previous commits — and enumerate, then bulk-delete all releases and tags via the GitHub REST API using the OAuth token. Moving to a different provider soon.